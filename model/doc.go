// Package model holds the shared vocabulary types (VLAN, Record, Settings,
// UpstreamTarget) written by the GUI via store and read by configgen and the
// localrecords plugin. It has no dependencies on other project modules.
//
// See docs/config-schema.md for the schema these types mirror.
package model
