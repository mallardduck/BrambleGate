# Build output
/out/
/dist/
*.exe
bramblegate

# Downloaded Tailwind standalone CLI (cmd/gentailwind fetches it on demand)
/bin/

# Go workspace sum (kept? — committed once deps exist; ignore local cache only)
go.work.sum

# Runtime config volume (regenerated; never committed)
/config/.runtime/

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db
# ACME integration test artifacts (Pebble CA)
deploy/test/*.pem

# Generated release notes (scripts/git-changelog output, release.yml)
/release-notes.md
